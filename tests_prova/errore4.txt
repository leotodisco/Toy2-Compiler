
%
 errore non si possono usare dichiarazioni nel body dell'otherwise.do
%

proc main():
    let var a, b^=1,2;\;
    go.when(a > b).loop{-->! "uno"; a^= a-1; }
    .when(a < b).loop{-->! "due"; a^= a+1;}
    .otherwise.do{-->!"sono uguali"; var d, c^=4,2;\};

endproc