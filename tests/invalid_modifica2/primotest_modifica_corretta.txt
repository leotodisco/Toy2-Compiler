
% programma principale %
proc main():

%ERRORE SEMANTICO%
    %qui c'è una constante anzichè una boolean expr nella prima condixione%
    let var a ^= 1;\;
    go.when(4).loop{
    -->! "ciao sono nel primo loop";
    a ^= a +1;

    }
    .when(a > 2 && a < 7 ).loop{
    -->! "SONO NEL SECONDO WHEN-LOOP";
    a ^= a+1;
    }
    .otherwise.do{
        a ^= 9;
        -->! $(a);
    };

endproc

