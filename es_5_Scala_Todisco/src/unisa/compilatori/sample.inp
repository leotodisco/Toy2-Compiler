

% fa somma e restituisce anche la taglia del numero, oltre che il risultato%
proc sommac(ab: integer, b:string, d:integer,out z:integer, out c:boolean):
	var a : boolean;\
endproc

proc main():
    sommac(12, "sort", 11, 10, true);
endproc